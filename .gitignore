__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
/venv
ffmpg_venv
optical_flow_venv

# Local notes — not for upstream
*.txt
!requirements.txt

# Video artifacts — large binaries, keep out of git
*.mp4
*.mov

# Model weights — auto-downloaded by ultralytics on first use
*.pt

# Run outputs — keep the folder but not its contents
/runs/*
!/runs/.gitkeep

# Results of Resource monitoring scripts
/logs
/plots
